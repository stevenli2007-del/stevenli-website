import Section from './Section'

// Task 1-4 实现正式内容；当前为结构占位
// About 用灰底（Design.md §1：About / Art 交替灰底制造节奏）

export default function About() {
  return (
    <Section id="about" tone="muted">
      <p>About</p>
    </Section>
  )
}
